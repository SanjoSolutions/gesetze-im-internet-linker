{
  "type": "module",
  "scripts": {
    "test": "jest",
    "build": "esbuild src/index.js --bundle --outdir=build",
    "build:watch": "esbuild src/index.js --watch --bundle --outdir=build"
  },
  "devDependencies": {
    "@babel/core": "^7.23.0",
    "@babel/preset-env": "^7.22.20",
    "@types/jest": "^29.5.5",
    "babel-jest": "^29.7.0",
    "esbuild": "^0.19.4",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
